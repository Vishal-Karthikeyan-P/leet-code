/**
Palindrome Number
Given an integer x, return true if x is a palindrome, and false otherwise.
**/
bool isPalindrome(int x) {
    signed long int num=0,rem,t=x;
    if(t<0)
        return 0;
    while(t!=0)
    {
        rem=t%10;
        num=num*10+rem;
        t/=10;
    }
    return num==x;
}

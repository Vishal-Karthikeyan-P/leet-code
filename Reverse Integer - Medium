#include <math.h>
int reverse(int x){
    signed long int y=0,temp;
    while(x!=0)
    {
        y*=10;
        y+=x%10;
        x/=10;
    }
    if((y<pow(-2,31)) || y>(pow(2,31)-1))
        return 0;
    return y;
}
